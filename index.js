/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import AuthUserApp from './src/AuthUserApp';

AppRegistry.registerComponent(appName, () => AuthUserApp);
